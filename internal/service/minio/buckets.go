package minio_service

const (
	AvatarsBucket  = "avatars"
	PostsBucket    = "posts"
	MessagesBucket = "messages"
	Apks           = "apks"
)

var BucketList = []string{AvatarsBucket, PostsBucket, MessagesBucket, Apks}
